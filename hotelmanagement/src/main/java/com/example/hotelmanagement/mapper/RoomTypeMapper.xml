<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.example.hotelmanagement.mapper.RoomTypeMapper">

    <select id="queryPriceByType" resultType="com.example.hotelmanagement.entity.RoomType">
        select * from roomType where typeName = #{typeName};
    </select>
    <select id="queryAll" resultType="com.example.hotelmanagement.entity.RoomType">
        select * from roomType;
    </select>

</mapper>